{
  "name": "rizz-server",
  "version": "1.0.0",
  "description": "Rizz call intelligence service - joins Daily rooms via REST/webhook, detects bounties live, streams SSE, generates post-call summaries",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node-dev src/index.ts"
  },
  "dependencies": {
    "@mistralai/mistralai": "latest",
    "@supabase/supabase-js": "^2.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.0",
    "groq-sdk": "latest"
  },
  "devDependencies": {
    "@types/cors": "*",
    "@types/express": "*",
    "@types/node": "*",
    "ts-node-dev": "*",
    "typescript": "^5.0.0"
  }
}
